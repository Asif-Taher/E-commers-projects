"use client"
import { ProductType } from '@/types'
import React from 'react'

const ProductCard = ({product}: {product:ProductType}) => {
  return (
    <div>
      <h1>This is the product card component</h1>
    </div>
  )
}

export default ProductCard
